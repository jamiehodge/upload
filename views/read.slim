- unless resource.complete?
  == slim :patch
== slim :delete

a href=url(resource.id.to_s + '/media') download

dd
  dt id
  dd = resource.id
  dt name
  dd = resource.name
  dt types
  dd = resource.type
  dt size
  dd = resource.size
  dt complete?
  dd = resource.complete?